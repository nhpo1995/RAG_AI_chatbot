version: '3.8'
# Docker services for AI Document Assistant
# Note: Unstructured API has been removed, using Docling instead
services:
  # Add other services here if needed in the future
  # For now, only Qdrant is needed for vector storage
